export default function ResumeCard() {
  // component for show resume of peoples like card
  // has 4 prop: name, id, bestSkill, employmentStatus, canSave
  // canSave is mean this has saveIcon & onClick action
  return <></>;
}
